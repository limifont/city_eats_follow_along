<h1>Cities</h1>
<hr />

<% if @cities.any? %>
  <ul>
    <% @cities.each do |city| %>
      <li>
        <%= link_to city.name, city_path(city) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3>No cities. Please add one.</h3>
<% end %>

<%= link_to 'Create a city', new_city_path %>